name: mock
version_probe: "python --version"
adapter: "tests/fixtures/workers/mock_adapter.py"
lanes: [gate, discovery]
capabilities:
  - id: demo.echo
    oracle_kinds: [trivial, checks]
    verdict_sources: [deterministic_assert]
    parallel_safe: true
  - id: security.stub
    oracle_kinds: [trivial]
    verdict_sources: [deterministic_assert]
    parallel_safe: true
requires: {env: [], binaries: []}
data_egress: []
cost_profile: {tokens_per_run: 0, typical_wallclock_s: 1}
